# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: rest2mcp-apim

metadata:
  template: azd-init

services:
  functions:
    project: ./src/ApimMcpDemo.Functions
    language: dotnet
    host: function
